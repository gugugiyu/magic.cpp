{
	"name": "server",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"start": "bun run src/index.ts",
		"check": "tsc --noEmit",
		"build": "echo 'Frontend must be built separately with: bun run build --workspace=@webui'"
	},
	"dependencies": {
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"bun-types": "^1.3.11",
		"hast": "^1.0.0",
		"mdast": "^3.0.0",
		"unist-util-visit-parents": "^6.0.2"
	}
}
