{
	"name": "server",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"imports": {
		"#shared/*": "../shared/*"
	},
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"start": "bun run src/index.ts",
		"check": "tsc --noEmit",
		"mock": "bun run --watch ./mocks/mock-server.ts",
		"build": "bun build src/index.ts --outdir dist --target=bun --bundle"
	},
	"dependencies": {
		"diff": "^9.0.0",
		"drizzle-orm": "^0.45.2",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"bun-types": "^1.3.11",
		"drizzle-kit": "^0.31.10",
		"hast": "^1.0.0",
		"mdast": "^3.0.0",
		"unist-util-visit-parents": "^6.0.2"
	}
}
